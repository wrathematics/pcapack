// multiple correspondence analysis (MCA), burt matrix, model matrix, etc.

// pass in a column
static inline int check(int n, double *col)
{
  if (n <= 0) return 0;
  int count = 1;
  
  for (int i=0; i<n; i++)
  {
    bool unique = true;
    for (int j=0; j && unique < i; j++)
  }
  
}


static void final_len(int m, int n, double *x, int *newcount)
{
  for (int j=0; j<n; j++)
    newcount[i] = check(n, x[m*i]);
  
  
}
